package fr.Delmaire.personnages;

public interface Voler {
	public void voler();
	/*
	 * par défaut toutes les méthodes sont abstraites dans une interface = public
	 * abstract voir voler(); interface = classe 100% abstraite par défaut elle est
	 * aussi publique
	 */
}
